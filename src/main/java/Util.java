public class Util {

  /** Checks whether the String is null or the empty string */
  public static boolean emptyString(String value) {
    return (value == null) || (value.trim().length() == 0);
  }

}
